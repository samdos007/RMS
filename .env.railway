# Railway Environment Variables Template
# Set these in Railway Dashboard -> Variables

# REQUIRED - Generate a secure 32+ character random string
SECRET_KEY=<GENERATE_RANDOM_32_CHAR_STRING>

# Production settings
DEBUG=false
COOKIE_SECURE=true

# Railway will set PORT automatically, but you can override
# PORT=8000

# Data directory - maps to Railway volume
DATA_DIR=/app/data

# CORS - Set to your Railway domain
# Example: CORS_ORIGINS=https://your-app.up.railway.app,https://www.your-app.up.railway.app
CORS_ORIGINS=https://${{RAILWAY_PUBLIC_DOMAIN}}

# Session settings
SESSION_MAX_AGE=604800

# Optional: PostgreSQL (if switching later)
# DATABASE_URL=postgresql://user:pass@host:port/db
