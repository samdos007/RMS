# Backend Configuration
# Copy this file to .env and update the values

# Required: Secret key for signing sessions (minimum 32 characters)
SECRET_KEY=change-this-to-a-random-32-char-secret-key

# Optional: Data directory for SQLite database and uploads
# DATA_DIR=./data

# Optional: Database URL (defaults to sqlite:///./data/rms.db)
# DATABASE_URL=sqlite:///./data/rms.db

# Optional: Session cookie settings
# SESSION_MAX_AGE=604800  # 7 days in seconds
